# coding: utf-8
#first write a list/array of zero
#then make them sparkle
#jk then use the random function
#to generate 15 * 100 list of values for progress at (time, person)
then print to file
#No let me write a function first:
def RandomDDD_t
#dummy variables of the coordinates are (t, p)
#p(0, p) must be 0;
#p(15, p) must be 1;
# p(n+1, p) > p(n, p) by random - 0.05
#if p>1 p = 1.
#if p<0, p= 0
ARRAY = [[0 for x in range(15)] for y in range (100)]
ARRAY
